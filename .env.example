# Cloudinary Configuration
# Get these values from your Cloudinary Dashboard: https://cloudinary.com/console

# Your Cloudinary cloud name (required)
REACT_APP_CLOUDINARY_CLOUD_NAME=your-cloud-name

# Upload preset name for unsigned uploads (required)
# Create this in your Cloudinary console under Settings > Upload > Upload presets
REACT_APP_CLOUDINARY_UPLOAD_PRESET=your-upload-preset

# API Key for signed operations (optional but recommended for production)
REACT_APP_CLOUDINARY_API_KEY=your-api-key

# API Secret (DO NOT expose in frontend - use only for server-side operations)
# CLOUDINARY_API_SECRET=your-api-secret